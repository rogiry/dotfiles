# 아래 항목은 저장소에만 존재하고 홈 디렉토리로 전개하지 않는다.
README.md
CLAUDE.md
docs
docs/**
Brewfile
bootstrap.sh
macos
macos/**

# 주의: .zshrc.local 은 여기 넣지 않는다.
# create_dot_zshrc.local 로 "없으면 최초 1회만 생성, 이후 절대 안 건드림"
# 동작을 얻는다. ignore 에 넣으면 그 생성이 아예 일어나지 않는다.
